#version 450
#extension GL_ARB_shading_language_include : enable

layout(local_size_x = 1) in;

#include "common/buffers.glsli"


void main() {
	if (gl_GlobalInvocationID.x == 0) {
		B_contour_pixel_attribs = ContourPixelAttribsBuffer(balloc_acquire(B_contour_balloc_buffer, B_contour_desc.num_contour_pixels * PER_ELEMENT_BYTES_CONTOUR_PIXEL_ATTRIBS_BUFFER));
	}
}
