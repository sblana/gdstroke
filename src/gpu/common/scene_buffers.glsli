#ifndef SCENE_BUFFERS_GLSLI
#define SCENE_BUFFERS_GLSLI

#include <common/scene_data.glsli>

layout(set = 0, binding = 0, std140) restrict readonly uniform SceneDataUniform {
	SceneData cur;
	SceneData prv;
} U_scene_data;

layout(set = 0, binding = 1, std140) restrict readonly uniform ConfigUniform {
	float depth_bias;
	float pad0;
	float pad1;
	float pad2;
} U_config;

#endif // !SCENE_BUFFERS_GLSLI
